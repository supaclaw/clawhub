# Local Docker Storage

This directory is bind-mounted into the ClawHub container at `/storage`.

Sample local skills live under `storage/skills/`.

Tree:

```text
storage/
  skills/
    hello-world/
    markdown-linter/
    release-notes-helper/
```

You can replace these demo skills with your own folders that contain a `SKILL.md` file and optional supporting text files.

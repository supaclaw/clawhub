# Review Rules

1. Flag skipped heading levels such as `##` followed by `####`.
2. Flag command examples that do not match the repository tooling.
3. Flag vague setup instructions that omit required environment variables.
4. Keep suggestions minimal and directly editable.
